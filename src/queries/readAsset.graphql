query ReadAssets($first: Int, $skip: Int) {
  pages: assets(first: $first, skip: $skip, orderBy:createdAt_DESC) {
    id
    updatedAt
    fileName
    size
    height
    fileName
    mimeType
    status
    createdAt
    width
    handle
    url
  }
}